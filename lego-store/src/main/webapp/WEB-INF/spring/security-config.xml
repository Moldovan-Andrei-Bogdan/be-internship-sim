<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans.xsd
http://www.springframework.org/schema/security
http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true" create-session="stateless">
        <custom-filter before="BASIC_AUTH_FILTER" ref="defaultJwtTokenFilter" />
        <intercept-url pattern="/users/login" access="isAuthenticated()" />
        <intercept-url pattern="/users/logout" access="isAuthenticated()" />
        <intercept-url pattern="/products/**" access="hasRole('USER')" />
        <http-basic />
        <csrf disabled="true"/>
    </http>

    <authentication-manager>
        <authentication-provider ref="defaultAuthenticationProvider">
        </authentication-provider>
    </authentication-manager>

</beans:beans>